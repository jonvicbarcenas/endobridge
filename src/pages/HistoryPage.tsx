import { Panel } from '../components/ui'

export function HistoryPage() {
  return (
    <Panel title="Session history">
      <p className="text-sm text-slate-600">
        Session history and local purge controls will be added in the history checkpoint.
      </p>
    </Panel>
  )
}
